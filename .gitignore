.claude/settings.local.json

# The installed desktop app (built with `pnpm desktop` in codebase/).
Aetherion.exe

# Shareable build output (`pnpm release` in codebase/).
release/
Aetherion-*-windows.zip
